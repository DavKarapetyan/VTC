@model List<NewsViewModel>
@{
    Layout = "_AdminLayout";
}
<a asp-controller="Admin" asp-action="ManageNews" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-success open-modal">Add</a>
<table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Title</td>
                <td>@item.Description</td>
                <td>
                    <a asp-controller="Admin" asp-action="ManageNews" asp-route-id="@item.Id" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-success open-modal">Edit</a>
                    <a asp-controller="Admin" asp-action="DeleteNews" asp-route-id="@item.Id" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-danger open-modal">Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>