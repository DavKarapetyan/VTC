@model List<EventViewModel>
@{
    Layout = "_AdminLayout";
}
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<a asp-controller="Admin" asp-action="ManageEvent" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-success open-modal">Add</a>
<table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Date</th>
            <th>Time</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Date</td>
                <td>@item.Time</td>
                <td>@item.Description</td>
                <td>
                    <a asp-controller="Admin" asp-action="ManageEvent" asp-route-id="@item.Id" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-success open-modal">Edit</a>
                    <a asp-controller="Admin" asp-action="DeleteEvent" asp-route-id="@item.Id" data-bs-toggle="modal" data-bs-target="#dialogDiv" class="btn btn-danger open-modal">Delete</a>                
                </td>
            </tr>
        }
    </tbody>

</table>